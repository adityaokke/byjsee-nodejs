const resolvers = {
  Query: {
    users(parent, args, context, info) {
      return [];
    },
  },
  Mutation: {
    register(parent, args, context, info) {
      return [];
    },
  },
};

module.exports = resolvers;
